
<div class="card mb-3" id="drty-card">
  <div class="text-center">
    <div class="col-md-2">
      <img src={{stamp.Photo}} class="card-img" alt="avatar" />
    </div>

    <h2 style="color: black;">Photo by:
      {{user.first_name}}
      {{user.last_name}}
    </h2>
    <p>Capturing {{stamp.destination_name}}</p>
    <p>Summary: {{stamp.destination_notes}} </p>
    <p>Likes: {{stamp.likes}}</p>
    <p>
    </p>
  </div>
</div>